package ui.tests;

import static org.junit.Assert.*;

import java.util.logging.Level;
import java.util.logging.Logger;

import org.junit.Test;

import javafx.application.Application;
import javafx.fxml.FXMLLoader;
import javafx.scene.Scene;
import javafx.scene.layout.GridPane;
import javafx.scene.layout.VBox;
import javafx.stage.Stage;
import ui.game.W_FightConfirm;
import ui.game.W_TakeTreasure;
import uk.ac.aber.cs211.group07.system.Board;
import uk.ac.aber.cs211.group07.system.GameEngine;
import uk.ac.aber.cs211.group07.system.resources.CrewCard;
import uk.ac.aber.cs211.group07.system.resources.Position;


/**
 * Tests addressing W_FightConfirm and FightConfirm.fxml
 * @author Jake McDonagh
 * @version 0.1 - 01/05/17 - initial creation of test /jam93
 */
/*
public class TestFightConfirm extends javafx.application.Application {
	
	@Test
	public void test() {
		this.launch(null);
	}
	**/
	/**
	 * D
	 * @param stage
	 * @throws Exception
	 */
	
	//TEST NOT CURRENTLY IN USE
	//REMOVE BEFORE SUBMISSION
	/*
	@Override
	public void start(Stage stage) throws Exception {
		GameEngine engine;
		String names[] = new String[4];
		names[0] = "name1";
		names[1] = "name2";
		names[2] = "name3";
		names[3] = "name4";
		engine = new GameEngine();
		engine.initGame(names);
		
try {
			
        	FXMLLoader loader = new FXMLLoader(TestTakeTreasure.class.getClassLoader().getResource("ui/FightConfirm.fxml"));
        	VBox page = (VBox) loader.load();
        	Board b = engine.getBoard();
       // 	engine.getCurrentPlayer().getShip().addTreasure(b.getTreasure(1));
        	CrewCard card = new CrewCard(2, true);
			engine.getPlayer(1).addCard(card);
			engine.getCurrentPlayer().addCard(card);
        	engine.getPlayer(1).getShip().addTreasure(b.getTreasure(10));
        	engine.getPlayer(1).getShip().addTreasure(b.getTreasure(11));
        	b.moveMapObject(engine.getCurrentPlayer().getShip(), new Position(3,4));
        	b.moveMapObject(engine.getPlayer(1).getShip(), new Position(3,4));
        	
        	//Set up the controller
        	((W_FightConfirm) (loader.getController())).setUpController(stage, stage, engine, new Position (4, 4));
        	
        	
        	
        	Scene scene = new Scene(page, 460, 377);
            stage.setScene(scene);
            stage.setTitle("Set fight");
            stage.setResizable(false);
            
            //Show the window
            stage.show();   
        } catch (Exception ex) {
            Logger.getLogger(Application.class.getName()).log(Level.SEVERE, null, ex);
        }
		
		
		
		
	
		
		
	}

}
**/

